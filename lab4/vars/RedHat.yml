---
packages:
  web_package: httpd
  ftp_package: vsftpd
  db_package: mariadb-server
  firewall_package: firewalld

services:
  web_service: http
  ftp_service: vsftpd
  db_service: mysql
  firewall_service: firewalld
